import React from "react";
import styled from "styled-components";
const Devices = () => {
  return <Section id="devices">Devices</Section>;
};

const Section = styled.section``;

export default Devices;
